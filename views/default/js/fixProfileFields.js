/**
 * Fixes event calendar
 */
function fixProfileFields() {

    // remove private profile fields

    //$('.custom-field-birthday, .custom-field-street, .custom-field-zip, .custom-field-city').remove();

}


$.when($.ready).then(fixProfileFields);
